[package]
name = "applechu"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "AppleChu"
crate-type = ["cdylib"]

[dependencies]
toml = "0.8"
once_cell = "1"

[profile.release]
lto = true
opt-level = "s"
